#ifndef KIRB_BRIDGE_HTTPD_H
#define KIRB_BRIDGE_HTTPD_H

/** @file bridge_httpd.h
 *  @brief HTTP server entry point for the ESP32 bridge dashboard.
 */

/** Start the bridge dashboard and API HTTP server. */
void startBridgeServer();

#endif // KIRB_BRIDGE_HTTPD_H
